<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}Memtext{% endblock title %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap styles -->
  <link href="/vendor-css/bootstrap.min.css" rel="stylesheet">
  <!-- Jquery -->
  <script defer src="/vendor-js/jquery.min.js"></script>
  <!-- Bootstrap js -->
  <script defer src="/vendor-js/bootstrap.min.js"></script>
  <!-- Common page styles -->
  <link rel="stylesheet" href="/css/common.css">
  <!-- Additional styles and scripts for concrete page -->
  {% block extra %}{% endblock extra %}
</head>
<body>
  <div class="wrapper-outer">
    <div class="wrapper-inner">
      <nav class="navbar navbar-default navbar-static-top">
        <div class="container">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Memtext</a>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <li><a href="/about">О сайте</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              {% if loginManager.isLogged() %}
                <li>
                  <form action="/logout" method="POST" name="logoutForm" class="logout-form">
                    {% include 'csrf_inputs.twig' %}
                    <button type="submit" name="logoutForm[logout]"
                      class="btn btn-link logout-button">Выйти</button>
                  </form>
                </li>
              {% else %}
                <li><a href="/login">Вход</a></li>
                <li><a href="/register">Регистрация</a></li>
              {% endif %}
            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container -->
      </nav>

      <div class="container">
          {% block content %}{% endblock content %}
      </div>

    </div><!-- /.wrapper-inner -->
  </div><!-- /.wrapper-outer -->

  <nav class="footer-static-bottom">
    <div class="container text-center">
      <div class="copyright">&copy; Memtext {{ "now"|date("Y") }}, все права защищены.</div>
    </div>
  </nav>

</body>
</html>
